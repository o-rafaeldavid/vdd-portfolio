export type ViewportProps = {
    children?: React.ReactNode,
    margin?: boolean,
    className?: string
}

export type ViewportWithSafespaceProps = {
    children?: React.ReactNode,
    marginViewport?: boolean,
    marginSafespace?: boolean,
    className?: string
}